[build]
builder = "dockerfile"
dockerfilePath = "packages/website/Dockerfile"

[deploy]
startCommand = "bun --cwd packages/website run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
