[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/app/Dockerfile"

[deploy]
startCommand = "bun --cwd packages/app run start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
